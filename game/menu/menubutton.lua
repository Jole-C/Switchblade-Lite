local menuButton = class{
    drawColour = {},
    selectedColour = {},
    isSelected = false,

    update = function(self)

    end,
    
    draw = function(self)

    end,

    execute = function(self)

    end
}

return menuButton